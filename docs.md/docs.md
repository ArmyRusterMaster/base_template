базовый шабблон для фуллстак проектов на раст лептос аксум

- сделать герйсфул шатдаун
- сделать общий скрипт подготовки  и установки всех компонентов по типу тайлвинд, карго лептос, васм таргета

- конфиги через ямл и энвв с компайл тайм валидацией

- докер и докер композ

- си сд

- 4. Оптимизированный Базовый Asset PipelineШаблонная обработка статики, чтобы не настраивать это каждый раз:Настроенный сжатый пре-рендеринг (gzip/brotli статических файлов при сборке продакшена).Готовый механизм cache busting (добавление хэша к файлам стилей и JS, чтобы у пользователей не кешировался старый интерфейс после апдейта сааса). cargo-leptos умеет это из коробки, но в шаблоне нужно правильно прописать пути к ассетам.

# Быстрый аудит уязвимостей в зависимостях
	cargo audit