[package]
name = "base_template"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

# Воркспейс: приложение (этот пакет) + крейты с общими типами.
# resolver задан явно (edition 2021 подразумевает "2") — чтобы поведение резолва
# фич не менялось незаметно при будущих правках манифестов.
[workspace]
members = ["crates/core-shared"]
resolver = "2"

[dependencies]
leptos = { version = "0.8.0" }
leptos_router = { version = "0.8.0" }
axum = { version = "0.8.0", optional = true }
console_error_panic_hook = { version = "0.1", optional = true }
leptos_axum = { version = "0.8.0", optional = true }
leptos_meta = { version = "0.8.0" }
tokio = { version = "1", features = ["rt-multi-thread", "signal", "macros"], optional = true }
wasm-bindgen = { version = "0.2.106", optional = true }
serde = { version = "1", features = ["derive"] }
# YAML: serde_yaml заархивирован, serde_yml тоже помечен deprecated (шим над
# noyalib). serde_yaml_ng 0.10 — drop-in форк serde_yaml 0.9 с тем же API.
serde_yaml_ng = "0.10"
# Общие DTO (клиент + сервер) — отдельный крейт воркспейса.
core-shared = { path = "crates/core-shared" }
# leptos-use 0.19.x совместим с leptos 0.8 (см. docs/stack.md).
leptos-use = "0.19.2"
# thaw-ui (0.4.x) требует leptos 0.7 и с 0.8 несовместим — не используется.
# Интеграционные библиотеки добавляются в шаблонах-наследниках осознанно
# (с проверкой совместимости версий leptos).
tracing = { version = "0.1.44", optional = true }
tracing-subscriber = { version = "0.3.23", features = ["env-filter"], optional = true }
tower-http = { version = "0.6", features = ["request-id"], optional = true }
async-trait = { version = "0.1.92", optional = true }
serde_json = { version = "1.0.151", optional = true }
gloo-net = "0.7.0"

[target.'cfg(not(target_family = "wasm"))'.dependencies]
# Аллокатор только native SSR; wasm использует стандартный аллокатор Rust.
mimalloc = { version = "0.1.52", optional = true, default-features = false }

[features]
hydrate = [
    "leptos/hydrate",
    "dep:console_error_panic_hook",
    "dep:wasm-bindgen",
]
ssr = [
    "dep:mimalloc",
    "dep:axum",
    "dep:tokio",
    "dep:leptos_axum",
    "leptos/ssr",
    "leptos_meta/ssr",
    "leptos_router/ssr",
    "dep:tracing",
    "dep:tracing-subscriber",
    "dep:tower-http",
    "dep:async-trait",
    "dep:serde_json",
    "leptos-use/ssr",
]

# Defines a size-optimized profile for the WASM bundle in release mode
[profile.wasm-release]
inherits = "release"
opt-level = 'z'
lto = true
codegen-units = 1
panic = "abort"

[package.metadata.leptos]
# The name used by wasm-bindgen/cargo-leptos for the JS/WASM bundle. Defaults to the crate name
output-name = "base_template"

# The site root folder is where cargo-leptos generate all output. WARNING: all content of this folder will be erased on a rebuild. Use it in your server setup.
site-root = "target/site"

# The site-root relative folder where all compiled output (JS, WASM and CSS) is written
# Defaults to pkg
site-pkg-dir = "pkg"

# Tailwind CSS: входной файл (v4, CSS-first конфигурация). cargo-leptos сам
# скачивает standalone-бинарник Tailwind (версия — LEPTOS_TAILWIND_VERSION,
# по умолчанию v4.x) и кладёт результат в <site-root>/<site-pkg-dir>/<output-name>.css.
tailwind-input-file = "style/main.css"
# Assets source dir. All files found here will be copied and synchronized to site-root.
# The assets-dir cannot have a sub directory with the same name/path as site-pkg-dir.
#
# Optional. Env: LEPTOS_ASSETS_DIR.
assets-dir = "public"

# The IP and port (ex: 127.0.0.1:3000) where the server serves the content. Use it in your server setup.
site-addr = "127.0.0.1:3000"

# The port to use for automatic reload monitoring
reload-port = 3001

# [Optional] Command to use when running end2end tests. It will run in the end2end dir.
#   [Windows] for non-WSL use "npx.cmd playwright test"
#   This binary name can be checked in Powershell with Get-Command npx
end2end-cmd = "npx playwright test"
end2end-dir = "end2end"

#  The browserlist query used for optimizing the CSS.
browserquery = "defaults"

# The environment Leptos will run in, usually either "DEV" or "PROD"
env = "DEV"

# The features to use when compiling the bin target
#
# Optional. Can be over-ridden with the command line parameter --bin-features
bin-features = ["ssr"]

# If the --no-default-features flag should be used when compiling the bin target
#
# Optional. Defaults to false.
bin-default-features = false

# The features to use when compiling the lib target
#
# Optional. Can be over-ridden with the command line parameter --lib-features
lib-features = ["hydrate"]

# If the --no-default-features flag should be used when compiling the lib target
#
# Optional. Defaults to false.
lib-default-features = false

# The profile to use for the lib target when compiling for release
#
# Optional. Defaults to "release".
lib-profile-release = "wasm-release"
